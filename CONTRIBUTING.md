# Contributing

1. Fork & branch from `main`.
2. `uvicorn backend.app.main:app --reload` for local dev.
3. Ensure `black`/`ruff` pass (CI will check).
4. Open a PR with a clear description & screenshots (if UI).

Thanks!
